<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:background="?attr/selectableItemBackground"> <ImageView
    android:id="@+id/itemImagen"
    android:layout_width="80dp"  android:layout_height="120dp" android:scaleType="centerCrop"
    android:background="#DDDDDD" app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    android:contentDescription="Portada del libro"
    android:layout_marginEnd="8dp"/>

    <TextView
        android:id="@+id/itemTitulo"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:text="Título de prueba"
        android:textStyle="bold"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="@id/itemImagen"
        app:layout_constraintStart_toEndOf="@id/itemImagen"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/itemAutor"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Autor de prueba"
        android:textSize="14sp"
        android:textColor="#888888"
        app:layout_constraintTop_toBottomOf="@id/itemTitulo"
        app:layout_constraintStart_toEndOf="@id/itemImagen"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="4dp"/>

    <TextView
        android:id="@+id/itemResumen"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Resumen de prueba"
        android:textSize="12sp"
        android:textColor="#666666"
        android:maxLines="3" android:ellipsize="end" app:layout_constraintTop_toBottomOf="@id/itemAutor"
        app:layout_constraintStart_toEndOf="@id/itemImagen"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="4dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>